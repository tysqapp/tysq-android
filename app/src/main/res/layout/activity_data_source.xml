<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/iv_banner"
        android:layout_width="match_parent"
        android:layout_height="255dp"
        android:layout_marginTop="50dp"
        android:src="@drawable/img_data_cource" />

    <EditText
        android:id="@+id/et_data_source"
        style="@style/et_style"
        android:layout_below="@id/iv_banner"
        android:layout_marginTop="50dp"
        android:hint="@string/data_source_enter"
        android:paddingEnd="50dp"
        android:text=""  />

    <ImageView
        android:id="@+id/iv_close"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignTop="@id/et_data_source"
        android:layout_alignEnd="@id/et_data_source"
        android:padding="18dp"
        app:srcCompat="@drawable/ic_close_fill_dark_gray"
        android:visibility="gone"/>

    <TextView
        android:id="@+id/tv_confirm"
        android:layout_below="@id/et_data_source"
        style="@style/btn_blue_style"
        android:layout_marginStart="27.5dp"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="27.5dp"
        android:text="@string/data_source_commit" />

</RelativeLayout>